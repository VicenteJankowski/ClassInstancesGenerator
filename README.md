# ClassInstancesGenerator
API for generation many instances of provided Java class - it uses reflections to obtain field in provided class and basing on its type and annotations it generates random values.
